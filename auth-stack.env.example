# Append these values to the backend root .env when enabling Next.js + BetterAuth.
# Do not commit real secrets.

BETTER_AUTH_INTERNAL_URL=http://localhost:3000
FASTAPI_INTERNAL_AUTH_SECRET=replace-with-the-same-secret-as-web
